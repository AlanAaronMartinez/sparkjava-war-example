pipeline {
    agent { node { label 'AlanMartinez'}}
    stages {
        stage('primeros pasos') {
            steps {
                sh '''
                echo "hola"
                ls
                pwd
                '''
            }
        }
    }
}
